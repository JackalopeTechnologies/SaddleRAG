<!--
    RegistrationDialog.xaml
    Copyright © 2012–Present Jackalope Technologies, Inc. and Doug Gerard.
    SPDX-License-Identifier: MIT
    Licensed under the MIT License. See the LICENSE file in the repo root.
-->
<Window x:Class="SaddleRAG.Tray.RegistrationDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="SaddleRAG — register a tool"
        SizeToContent="Height"
        Width="640"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        ShowInTaskbar="False">
    <StackPanel Margin="16">
        <TextBlock x:Name="IntroText" TextWrapping="Wrap" Margin="0,0,0,12"/>
        <TextBlock x:Name="FirstLabel" Margin="0,0,0,4"/>
        <TextBox   x:Name="FirstValue" Margin="0,0,0,12"/>
        <TextBlock x:Name="SecondLabel" Margin="0,0,0,4"/>
        <TextBox   x:Name="SecondValue" Margin="0,0,0,12"/>
        <TextBlock TextWrapping="Wrap"
                   Margin="0,0,0,12"
                   Opacity="0.75"
                   Text="Leave both blank to clear the registration. SaddleRAG never installs, licenses, configures, or upgrades these tools — it only records what you tell it to run."/>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Save"   Width="90" Margin="0,0,8,0" IsDefault="True" Click="OnSave"/>
            <Button Content="Cancel" Width="90" IsCancel="True"/>
        </StackPanel>
    </StackPanel>
</Window>
